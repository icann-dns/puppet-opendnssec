---
version: 5
defaults:
  datadir: 'data'
  data_hash: 'yaml_data'
hierarchy:
  - name: "release"
    path: "os/%{os.name}/%{os.release.major}.yaml"
  - name: "name"
    path: "os/%{os.name}.yaml"
  - name: "family"
    path: "os/%{os.family}.yaml"
